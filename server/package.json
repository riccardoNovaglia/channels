{
  "name": "channels-server",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "test": "jest --watch src",
    "test:ci": "jest",
    "start": "nodemon src/index.js"
  },
  "dependencies": {
    "express": "^4.17.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.1"
  }
}
